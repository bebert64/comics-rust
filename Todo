Parsing
    Multiple Volumes will need to parse their issues completely
    Parse issues with + ??
    Parse Annual issues

Maintenance
    If archive status = to parse AND dir is in Comics OK, move back to Comics
    Get volumes with 0 books/issues
    Get volumes with more than 1 book and at least one book has no reading order AND no volume_number

Lib NAS
    is mounted ?
    mount
    rename folder /file and move to ok
    create zip and copy to ipad

lib api
    get metadata from folder name
