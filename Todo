Refacto issues
    if first and last issues are provided
        check if numero can be found in subdirs in order
        throw error if there are additional issues
    create issues without path if dir contains only files
Parse Single Issue book

Parse issues with + ??


If archive status = to parse AND dir is in Comics OK, move back to Comics


Lib NAS
    is mounted ?
    mount
    rename folder /file and move to ok
    create zip and copy to ipad

lib api
    get metadata from folder name
