Correct VolumeAndIssue
    map subdirs to issue to add path
    move additional files from subdir not in issues (if flag present in cli args) to root and 
        add them as additional_files

Migration
    reproduce schema's manual modifications
    db check
        volume_number NOT NULL => type = SingleVolume
        type = GraphicNovel => title NOT NULL
        ...
    add column "verified" for each archive, default false
Write in db info from parsed dir
Move to other folder
    script to revert all while in test? Maybe cut/paste all is enough





Lib NAS
    is mounted ?
    mount
    rename folder /file and move to ok
    create zip and copy to ipad

lib api
    get metadata from folder name
